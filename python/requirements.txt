# Core — zero required runtime dependencies (pure asyncio stdlib).
# Install extras as needed:
#   pip install synapse-orchestrator[openai]
#   pip install synapse-orchestrator[anthropic]
#   pip install synapse-orchestrator[all]

# Development / testing
pytest>=7.0
pytest-asyncio>=0.21
openai>=1.0
anthropic>=0.25
